# PriceLabs Payment Method Mockup

A static HTML recreation of PriceLabs' mobile Subscription & Payment Method billing screen, including the empty state and the full add-payment-method flow (card, bank account, PayPal).

Open `index.html` directly in a browser, or deploy as-is — no build step required.
